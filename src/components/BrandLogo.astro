---
import { Image } from "astro:assets";
import logoImg from "@/assets/images/logo.png";
const { class: className } = Astro.props;
---
<Image src={logoImg} alt="Logo" class={`${className || "h-10 w-auto"} rounded-full object-cover`} />
